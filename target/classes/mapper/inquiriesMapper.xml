<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="Inquiries">
    <select id="findByProductId" parameterType="Long" resultType="inquiry">
        select * from product_inquiries where product_id = #{id}
    </select>
    <insert id="save" parameterType="inquiry">
        insert into product_inquiries (inquiry_id, product_id, user_id, inquiry_content)
        values (seq_inq.nextVAL, #{productId}, #{userId}, #{inquiryContent})
    </insert>
    <delete id="delete" parameterType="Long">
        delete from product_inquiries where inquiry_id = #{inquiryId}
    </delete>
    <update id="update" parameterType="inquiry">
        update product_inquiries set inquiry_content = #{inquiryContent} where inquiry_id
        = #{inquiryId}
    </update>

</mapper>
