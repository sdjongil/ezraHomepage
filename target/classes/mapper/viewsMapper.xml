<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="Views">
    <insert id="saveViews" parameterType="views">
        insert into likes_table(view_Id, board_Id, member_Nickname)
        values (views_seq.nextval, #{boardId}, #{memberNickname})
    </insert>
</mapper>











