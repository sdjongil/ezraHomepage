<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="Likes" >
    <insert id="saveLike" parameterType="likes">
        insert into likes_table(likeId, boardId, memberNickname)
        values (likes_seq.nextval, #{boardId}, #{memberNickname})
    </insert>
    <select id="findLike" parameterType="likes" resultType="Integer">
        select * from likes_table where boardId = #{boardId} and memberNickname = #{memberNickname}
    </select>
</mapper>
